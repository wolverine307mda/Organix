# ??? Server ?????????????????????????????????????????????
server.port=8080
spring.jackson.time-zone=Europe/Madrid

# ??? Database (PostgreSQL) ?????????????????????????????
spring.datasource.url=jdbc:postgresql://localhost:5432/organix_db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=organix_user
spring.datasource.password=organix_pass
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ??? JPA / Hibernate ???????????????????????????????????
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.time_zone=Europe/Madrid

# ??? Redis (Cache) ?????????????????????????????????????
spring.cache.type=redis
spring.data.redis.host=${SPRING_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
# spring.data.redis.password=                      # Solo si tu Redis tiene contrase�a
spring.data.redis.timeout=2000

# ??? Seguridad (JWT, CORS) ?????????????????????????????
spring.security.user.name=admin
spring.security.user.password=admin

# CORS (si usas WebMvcConfigurer puedes quitar esto)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*

# JWT
jwt.secret=miClaveSecretaEsEsta
# en milisegundos (24h)
jwt.expiration=86400000

# ??? Swagger / OpenAPI ????????????????????????????????
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# ??? Email (SMTP Gmail) ???????????????????????????????
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=movietrip.mda@gmail.com
spring.mail.password=bntf usjv kdoc dtff
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ??? Upload ????????????????????????????????????????????
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB




# ??? Logging ???????????????????????????????????
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF
logging.level.org.hibernate.orm.jdbc.bind=OFF
logging.level.org.hibernate.orm.jdbc.extract=OFF
logging.level.org.springframework.orm.jpa=WARN
logging.level.org.springframework.transaction=WARN
